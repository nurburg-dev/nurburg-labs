StylesPath = .vale/styles

MinAlertLevel = suggestion

Packages = Google, proselint

Vocab = nurburg

[*.md]
BasedOnStyles = Vale, Google, proselint
TokenIgnores = (\*\*[^*]+\*\*|\`[^`]+\`|\[.+?\]\(.+?\))

# Relax rules that conflict with technical writing conventions
Google.Passive = NO
Google.We = NO
proselint.Typography = NO
